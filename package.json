{
  "name": "krill",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
