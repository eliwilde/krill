{
  "name": "krill",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy",
    "prepare": "node build.js"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
